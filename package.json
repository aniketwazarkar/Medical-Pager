{
  "name": "medical-pager-root",
  "version": "1.0.0",
  "description": "Medical Pager Multi-Tenant Platform",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"cd backend && go run ./cmd/server\" \"cd frontend && npm run dev\"",
    "install:all": "cd backend && go mod tidy && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
